name: Build site

on:
  push:
    branches: [main]
    paths:
      - 'data/pronunciations.tsv'
      - 'tools/build-site.sh'
      - '.github/workflows/build-site.yml'
      - 'docs/v2/audio.jsx'
      - 'docs/v2/sections-1.jsx'
      - 'docs/v2/sections-2.jsx'
      - 'docs/v2/eggs.jsx'
      - 'docs/v2/app.jsx'
      - 'tools/build-v2-data.py'
      - 'tools/build-v2-bundle.sh'
      - 'tools/build-entry-dates.py'
      - 'tools/test_build_entry_dates.py'
      - 'tools/build-scoreboard.py'
      - 'tools/build-seo.py'
      - 'tools/test_build_seo.py'
      - 'tools/make-audio-all.py'
      - 'tools/make-og.py'
      - 'tools/make-og-all.py'
      - 'tools/test-v2-audio.mjs'
      - 'tools/test_build_v2_data.py'
      - 'tools/test_make_og_all.py'
  workflow_dispatch:

concurrency:
  group: build-site
  cancel-in-progress: false

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'

      - name: Install Pillow
        run: pip install Pillow

      - name: Install ffmpeg (for audio rendering)
        run: brew install ffmpeg

      - name: Build static site
        run: bash tools/build-site.sh

      - name: Render per-word audio (macOS `say` → mp3; idempotent, fills only new words)
        run: python3 tools/make-audio-all.py

      - name: Regenerate global OG card (docs/og.png — keeps "and N more." in sync with dict)
        run: python3 tools/make-og.py

      - name: Regenerate per-word OG images
        run: python3 tools/make-og-all.py

      - name: Detect changes
        id: changes
        run: |
          if [ -z "$(git status --porcelain -- docs/ data/entry-dates.tsv)" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit + push if changed
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/ data/entry-dates.tsv
          git commit -m "build: regenerate site (auto)" -m "Triggered by ${{ github.event.head_commit.id }}"
          for i in 1 2 3; do
            git pull --rebase --autostash origin main && git push && break || sleep 5
          done
